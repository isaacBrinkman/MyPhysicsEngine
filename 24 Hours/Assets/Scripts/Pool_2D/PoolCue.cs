using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// This class is in charge of the cue (stick thingy in pool)
/// should rotate with the mouse
/// when the player holds the mouse button can be dragged back and 'charged up'
/// farther back higher velocity
/// not sure this will work with circle colliders
/// may have to implement boc collider as well as kinematic objects
/// </summary>
public class PoolCue : MonoBehaviour
{
    float maxPower;

    // Start is called before the first frame update
    void Start()
    {
           
    }

    // Update is called once per frame
    void Update()
    {
        // forget how to say follow the mouse point around something with quaternions
        
    }
}
